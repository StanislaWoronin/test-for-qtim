import { SessionEntity } from './session.entity';
import { UserEntity } from './user.entity';
import { NewsEntity } from './news.entity';

export const entities = [NewsEntity, SessionEntity, UserEntity];
